# the normal cloud setup. python 3.13, NOT 3.14 (no wheels yet)

# audio in and out
sounddevice>=0.5.0        # this is the portaudio wrapper
numpy>=1.26

# deciding when somebody is talking
onnxruntime>=1.17         # runs the little silero model without needing torch
webrtcvad-wheels>=2.0.14  # backup vad, no download needed

# sample rate conversion (the tts comes back at 24k, devices want other things)
soxr>=0.5.0

# the actual apis
openai>=1.68
httpx>=0.27

# config and terminal stuff
PyYAML>=6.0
python-dotenv>=1.0
rich>=13.7                # the pretty two column view

# uncomment these if you want them
# deepl>=1.18
# elevenlabs>=1.0
