# extra stuff for running it completely offline.
# install on top of the normal ones:
#   pip install -r requirements.txt -r requirements-local.txt
#
# warning: these download multi GB models the first time

# local speech recognition
faster-whisper>=1.1.0     # whisper but through ctranslate2, way faster.
                          # "small" on cpu int8 is still nearly real time

# local translating
argostranslate>=1.9       # about 100mb per language pair, cpu only

# local voices
piper-tts>=1.2            # about 60mb per voice and its really fast

# windows + nvidia gpu only. without these ctranslate2 fails with
# "Library cublas64_12.dll is not found" on a completely fine graphics card
nvidia-cublas-cu12; platform_system == "Windows"
nvidia-cudnn-cu12; platform_system == "Windows"
